# 基于 1280x720 分辨率基准提取的 UI 坐标配置
BASE_LAYOUT = {
    # 翻页网格起点 (第一排第一列基质的中心点)
    "grid_p11": (71, 130),

    # 网格间距 (相邻两个基质的 X/Y 轴距离)
    "grid_delta": (106, 106),

    # 单个基质的识别框大小
    "matrix_size": (90, 90),

    # 词条识别区 ROI (x, y, 宽, 高)，包含 Slot 1, 2, 3 及等级区域
    "roi": (990, 230, 260, 130),

    # 锁定与废弃按钮中心坐标
    "lock_btn": (1227, 190),
    "discard_btn": (1205, 190),

    # 翻页滑动相关坐标与距离
    "swipe_start": (135, 191),
    "swipe_dist_first": 116,
    "swipe_dist_next": 115,

    # 扫描区域 ROI
    "roi_row1": (18, 72, 956, 119),  # 仅限第一行，用于常规探测
    "roi_final": (18, 72, 956, 570)  # 全局扫描，用于最后不足一页的情况
}