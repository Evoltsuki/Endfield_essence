# ==========================================
# 基于 MaaEnd 官方 EssenceFilter.json 提取的基准坐标
# 基准开发分辨率严格锁定为：1280x720
# ==========================================

BASE_LAYOUT = {
    # 1. 翻页网格起点 (第一排第一列基质的中心点)
    "grid_p11": (71, 130),

    # 2. 网格间距 (相邻两个基质的 X/Y 轴距离)
    "grid_delta": (106, 106),

    # 3. 单个基质的框大小 (用于切图判断金色)
    "matrix_size": (90, 90),

    # 4. 词条识别区 ROI (x, y, 宽, 高)
    # 包含 Slot 1, 2, 3 及等级区域
    "roi": (990, 230, 260, 130),

    # 5. 锁定按钮坐标 (x, y 中心点)
    "lock_btn": (1227, 190),

    # 6. 垃圾按钮坐标 (x, y 中心点)
    "discard_btn": (1205, 190),

    # 7. 翻页滑动起点
    "swipe_start": (135, 191),

    # 8. 翻页滑动距离 (Y轴滑动像素)
    "swipe_dist": 116
}